#!/usr/bin/env bash
set -e

NOTEBOOKS_PATH="${HOME}/Dropbox/ipython-notebooks"
IPYTHON_PATH="${HOME}/dev/myipy"

if [ ! -d "${NOTEBOOKS_PATH}" ]
then
	mkdir "${NOTEBOOKS_PATH}"
fi
cd "${NOTEBOOKS_PATH}"

cd "${IPYTHON_PATH}"
make run
